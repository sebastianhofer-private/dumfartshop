
plugin.tx_whocatmenu {
	view {
		templateRootPath = {$plugin.tx_whocatmenu.view.templateRootPath}
		partialRootPath = {$plugin.tx_whocatmenu.view.partialRootPath}
		layoutRootPath = {$plugin.tx_whocatmenu.view.layoutRootPath}
	}
	persistence {
		storagePid = {$plugin.tx_whocatmenu.persistence.storagePid}
	}
	features {
		# uncomment the following line to enable the new Property Mapper.
		# rewrittenPropertyMapper = 1
	}

	settings {
		storagePid = {$plugin.tx_whocatmenu.settings.storagePid}
		rootParent = {$plugin.tx_whocatmenu.settings.rootParent}
		maxMenuDepth = {$plugin.tx_whocatmenu.settings.maxMenuDepth}
		menuType = {$plugin.tx_whocatmenu.settings.menuType}
		listPageUid = {$plugin.tx_whocatmenu.settings.listPageUid}
	}
}

plugin.tx_whocatmenu._CSS_DEFAULT_STYLE (
	textarea.f3-form-error {
		background-color:#FF9F9F;
		border: 1px #FF0000 solid;
	}

	input.f3-form-error {
		background-color:#FF9F9F;
		border: 1px #FF0000 solid;
	}

	.tx-who-cat-menu table {
		border-collapse:separate;
		border-spacing:10px;
	}

	.tx-who-cat-menu table th {
		font-weight:bold;
	}

	.tx-who-cat-menu table td {
		vertical-align:top;
	}

	.typo3-messages .message-error {
		color:red;
	}

	.typo3-messages .message-ok {
		color:green;
	}

)

 # Module configuration
module.tx_whocatmenu {
	persistence {
		storagePid = {$module.tx_whocatmenu.persistence.storagePid}
	}
	view {
		templateRootPath = {$module.tx_whocatmenu.view.templateRootPath}
		partialRootPath = {$module.tx_whocatmenu.view.partialRootPath}
		layoutRootPath = {$module.tx_whocatmenu.view.layoutRootPath}
	}
}

config.tx_extbase {
	persistence {
		classes {
			\TYPO3\CMS\Extbase\Domain\Model\Category {
				0 = WHO\WhoCatMenu\Domain\Model\Category
			}

			WHO\WhoCatMenu\Domain\Model\Category {
				mapping {
					tableName = sys_category
					columns {
						parent.mapOnProperty = parent
					}
				}
			}
		}
	}

	objects {
		\TYPO3\CMS\Extbase\Domain\Repository\CategoryRepository {
			className = WHO\WhoCatMenu\Domain\Repository\CategoryRepository
		}
	}
}
